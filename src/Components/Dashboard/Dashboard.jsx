import React from 'react';
import { getProfileData } from '../../Api/services/authService';

const Dashboard = () => {
   return (
      <div>
         Dashboard
      </div>
   );
};

export default Dashboard;